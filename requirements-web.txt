# Runtime dependencies for the deployed API (agora.backend.infrastructure.web.api).
# Excludes torch/jupyter/ipykernel: those are for offline LightGCN training in
# notebooks/ only (see AGENTS.md) and never import at request time — keeping
# them out of the Docker image avoids a ~1GB+ unused install.
# GitHub Actions ingestion (.github/workflows/ingestion.yml) still installs
# the full requirements.txt, since it does call into the training/embedding path.

pydantic>=2.10.0
pydantic-settings>=2.7.0
openai>=1.55.0
httpx>=0.28.0
duckduckgo_search>=6.2.0
numpy>=2.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
psycopg[binary]>=3.2
psycopg-pool>=3.2
bcrypt>=4.0
